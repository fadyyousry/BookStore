<div class="dropdown-menu megamenu" role="menu">
    <div class="row">
        <% authors.in_groups(4, false) do |column| %>
            <div class="col-md-3">
                <% column.each do |author| %>
                    <%= link_to author.name, author, class: "nav-link"%>
                <%end%>
            </div>
        <% end %>
    </div>
</div>