var pk = '<%= Rails.application.credentials.stripe[Rails.env.to_sym][:publishable_key] %>';
var stripe = Stripe(pk);
stripe.redirectToCheckout({sessionId: '<%= @session.id %>'}).then(function (result) {});